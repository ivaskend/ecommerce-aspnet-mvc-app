@model IEnumerable<AlbumGenre>
@{
    ViewData["Title"] = "List of Album genres";
}

<div class="row">
    <div class="col-md-10 offset-md-1">
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <b> @Html.DisplayFor(modelItem => item.Name)</b>  
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Description)

                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-warning"><i class="bi bi-pencil-square"></i>Промени</a> |
                            <a class="btn btn-outline-info"><i class="bi bi-eye"></i>Повеќе детали</a> |
                            <a class="btn btn-outline-danger"><i class="bi bi-trash"></i>Избриши</a>

                        </td>


                    </tr>
                }
            </tbody>
        </table>


    </div>
</div>